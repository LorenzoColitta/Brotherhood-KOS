name = "brotherhood-kos-api"
main = "src/api/worker.js"
compatibility_date = "2024-01-01"

# Environment variables (set these via Wrangler or Cloudflare Dashboard)
# Do NOT commit actual secrets here - use `wrangler secret put` command

[env.production]
vars = { }

# Example of setting secrets:
# wrangler secret put API_SECRET_KEY
# wrangler secret put MONGODB_DATA_API_KEY
# wrangler secret put MONGODB_DATA_API_URL

# Example vars (non-secret configuration):
# vars = { MONGODB_DATABASE = "brotherhood-kos", MONGODB_DATASOURCE = "Cluster0" }
