ì[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "bash -lc 'curl -Ls https://cli.doppler.com/install.sh | sh && doppler configure set token $DOPPLER_SERVICE_TOKEN --no-local-config && doppler run -- npm start'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
