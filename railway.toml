[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "bash -lc 'doppler run --token \"$DOPPLER_SERVICE_TOKEN\" -- npm start'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
